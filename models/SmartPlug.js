class SmartPlug {

    constructor(id, identifier, name, online) {
        this.id = id,
        this.identifier = identifier,
        this.name = name,
        this.online = online;
    }
}

module.exports = SmartPlug;